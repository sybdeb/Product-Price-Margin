# -*- coding: utf-8 -*-

from . import product_margin_config
from . import product_template

# product_public_category is only loaded if website_sale module is installed
# This is handled by having it as a separate optional file
